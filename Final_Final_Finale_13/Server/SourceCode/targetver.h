#pragma once

// Chỉ định phiên bản tối thiểu của Windows mà ứng dụng sẽ nhắm đến.
// Để nhắm đến Windows 7 hoặc các phiên bản mới hơn, đặt giá trị `_WIN32_WINNT` thành 0x0601 (Windows 7).

#include <SDKDDKVer.h>
